# Database Configuration
DB_NAME=ahamo_category_db
DB_USERNAME=ahamo_user
DB_PASSWORD=ahamo_password

# Inventory Mock Configuration
# Set to 'true' to simulate inventory service failures for testing error handling
SIMULATE_FAILURE=false

# Service Configuration
# NEXT_PUBLIC_API_BASE_URL is set in docker-compose.yml
# INVENTORY_SERVICE_URL is set in docker-compose.yml
